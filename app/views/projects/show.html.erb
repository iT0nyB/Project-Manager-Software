<p id="notice"><%= notice %></p>

<div class="table-responsive">
<table class="table table-bordered table-hover">
<tr><td><strong>Title:</strong></td><td><%= @project.title %></td></tr>
<tr><td><strong>Budget:</strong></td><td>$
  <%= @project.budget %>
</td></tr>

<tr><td><strong>Ongoing Cost:</strong></td><td>$
  <%= @project.ongoingcost %>
</td></tr>
</table>
</div>

<h2>Milestones</h2>
<div id="Milestones">
    <%= render :partial => @project.milestones %>
</div>

<%= link_to 'New Milestone', new_milestone_path(:project_id => @project) %>

<br/>
<%= link_to 'Edit', edit_project_path(@project) %> |
<%= link_to 'Back', projects_path %>
